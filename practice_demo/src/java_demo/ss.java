package java_demo;

import java.util.logging.Level;
import java.util.logging.Logger;

public class ss {
	
//	Level level = Level.toLevel(Level.DEBUG);
//	Logger logger = LoggerManager.getLogger("package");
//	logger.setLevel(level);//设置包的日志级别
//	LoggerManager.getRootLogger().tosetLevel(level);//设置全局日志级别
}
